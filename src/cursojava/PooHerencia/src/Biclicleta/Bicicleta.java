/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package Biclicleta;
/*
public class Bicicleta {
    private int velocidadActual;
    private int platoActual;
    private int piñonActual;

    public Bicicleta(int velocidadActual, int platoActual, int piñonActual) {
        this.velocidadActual = velocidadActual;
        this.platoActual = platoActual;
        this.piñonActual = piñonActual;
    }

    public Bicicleta() {
    }
        
     public void acelerar() throws Exception{
        //dobla la velocidad actual no puede ser mayor de 100 
         if (velocidadActual < 100) {
             velocidadActual += 10;
         } else if (velocidadActual >= 100) {
             throw new Exception("La velocidad no puede ser mayor de 100km");
         }
    }
    
    public void frenar() throws Exception{
        //reduce a la mitad la velocidad actual no puede ser menor de 0
        if (velocidadActual > 1) {
             velocidadActual += 10;
         } else if (velocidadActual <= 1 ) {
             throw new Exception("La velocidad no puede ser menos de 0km");
         }
    }
    
    public void cambiarPlato(int plato){
        //cambia la propiedad plato 
        platoActual = plato;
        System.out.println("Hemos cambiado el " + platoActual + " por el " + plato);
    }
    
    public void cambiarPiñon(int piñon){
        //ajusta el piñón actual según el  parámetro  recibido.
        piñonActual = piñon;
        System.out.println("Hemos ajustado el " + piñonActual + " con el " + piñon);
    } 

}*/
